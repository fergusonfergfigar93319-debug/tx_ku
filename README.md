# tx_ku

腾讯开悟比赛项目 — 元流同频 Web 工作台（`web/` 目录）。

## 开发

```bash
cd web
npm install
npm run dev
```

浏览器访问开发服务器提示的本地地址（默认 `http://127.0.0.1:5173`）。

## 构建

```bash
cd web
npm run build
```

产物输出至 `web/dist/`。
