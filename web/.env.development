VITE_API_BASE_URL=/api/v1

# 模拟数据总开关：true 时所有 API 走本地 mock，不请求后端
VITE_USE_MOCK=true

# 可选：开发者通道「测试账号登录」用（勿提交密钥）
# VITE_DEV_EMAIL=dev@example.com
# VITE_DEV_PASSWORD=your-password
# 可选：模拟会话 token（默认 dev-quick-token）
# VITE_DEV_QUICK_TOKEN=dev-quick-token
